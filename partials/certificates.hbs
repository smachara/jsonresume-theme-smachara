  <spam class="before"></spam>
  {{!-- <p class="after">Break after</p> --}}
<section class="scale-cv certificates section" id="certificates">
    <h2 class="section-title">Certificats</h2>
    {{#resume.certificates}}
    <div class="certificates__container bd-grid">
        <div class="certificate__content">
            <h3 class="certificate__title">
                {{name}} | {{date}}
            </h3>
            <p class="certificete__description">
                {{issuer}}<br>
                <a class="certificate__url" href="{{url}}">{{url}}</a>
            </p>
        </div>
    </div>
    {{/resume.certificates}}
</section>

